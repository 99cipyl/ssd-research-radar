# SSD Research Radar state

This orphan branch contains the durable SQLite state used by the scheduled
GitHub Actions sync. It stores public-source metadata only and is not deployed
by GitHub Pages. The workflow updates this branch only when research content,
source mappings, version snapshots, or feed events materially change.
